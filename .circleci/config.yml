version: 2.1
orbs:
  aws-s3: circleci/aws-s3@2.0.0

executors:
  python:
    docker:
      - image: circleci/python:2-node

defaults:
  - &restore-cache
    restore_cache:
      name: Restore Yarn Package Cache
      keys:
        - cache-{{ checksum "yarn.lock" }}
  - &save-cache
    save_cache:
      name: Save Yarn Package Cache
      key: cache-{{ checksum "yarn.lock" }}
      paths:
        - /home/circleci/project/node_modules
  - &restore-app-build
    restore_cache:
      name: Restore build folder from cache
      keys:
        - cache-{{ .Branch }}
  - &save-app-build
    save_cache:
      name: Save build folder into cache
      key: cache-{{ .Branch }}
      paths:
        - /home/circleci/project/packages/app/build

jobs:
  build:
    executor: python
    steps:
      - checkout
      - *restore-cache
      - *restore-app-build
      - run:
          command: yarn
          name: Install dependencies
      - run:
          command: yarn build
          name: Compile
      - *save-cache
      - *save-app-build

  test:
    executor: python
    steps:
      - checkout
      - *restore-cache
      - *restore-app-build
      - run: yarn test

  deployS3:
    executor: python
    steps:
      - checkout
      - *restore-cache
      - *restore-app-build
      - aws-s3/sync:
          from: ./packages/app/build
          to: 's3://zsalab.com'
          arguments: |
            --acl public-read \
            --cache-control "max-age=86400" \
            --exclude ".git/*" \
            --exclude ".gitignore" \
            --exclude ".circleci/*" \

workflows:
  deploy:
    jobs:
      - build
      - test:
          requires:
            - build
      - deployS3:
          requires:
            - test
          context: aws
          filters:
            branches:
              only: main
