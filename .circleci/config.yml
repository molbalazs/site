version: 2.1
orbs:
  aws-s3: circleci/aws-s3@2.0.0

executors:
  python:
    docker:
      - image: circleci/python:2-node

defaults:
  - &restore-cache
    restore_cache:
      name: Restore Yarn Package Cache
      keys:
        - cache-2-{{ checksum "yarn.lock" }}
  - &save-cache
    save_cache:
      name: Save Yarn Package Cache
      key: cache-2-{{ checksum "yarn.lock" }}
      paths:
        - /node_modules
        - /build

jobs:
  build:
    working_directory: /home/circleci/project/packages/app
    executor: python
    steps:
      - checkout
      - *restore-cache
      - run:
          command: yarn
          name: Install dependencies
      - run:
          command: yarn build
          name: Compile
      - *save-cache

  test:
    working_directory: /home/circleci/project/packages/app
    executor: python
    steps:
      - checkout
      - *restore-cache
      - run: yarn test

  deployS3:
    working_directory: /home/circleci/project/packages/app
    executor: python
    steps:
      - checkout
      - *restore-cache
      - aws-s3/sync:
          from: ./packages/app/build
          to: "s3://zsalab.com"
          arguments: |
            --acl public-read \
            --cache-control "max-age=86400" \
            --exclude ".git/*" \
            --exclude ".gitignore" \
            --exclude ".circleci/*" \

workflows:
  version: 2

  deploy:
    jobs:
      - build
      - test:
          requires:
            - build
      - deployS3:
          requires:
            - test
          context: aws
          filters:
            branches:
              only: main
